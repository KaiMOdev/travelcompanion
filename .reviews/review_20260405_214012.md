# Code Review — last commit

**Model**: gpt-4.1  
**Date**: 2026-04-05 21:40

---

**[Bugs & Correctness] severity: medium**  
`voxlingo/server/index.ts:~120` — API key authentication is skipped if `SERVER_API_KEY` is not set, even in production  
> The middleware currently skips API key authentication if `SERVER_API_KEY` is not set, which could accidentally leave the API open in production if the environment variable is missing. This is a security risk.  
> **Suggestion:** Fail fast and refuse to start the server if `SERVER_API_KEY` is missing in production (already partially done, but double-check logic to ensure no bypass).

---

**[Security] severity: high**  
`voxlingo/server/index.ts:~60` — CORS configuration allows all origins in development  
> While this is acceptable for local development, ensure that this cannot be accidentally deployed to production. The code throws if `ALLOWED_ORIGINS` is empty in production, but review deployment scripts to ensure this is always set.  
> **Action:** Add logging or a startup check to make it obvious when running with open CORS, and consider a default deny policy.

---

**[Security] severity: medium**  
`voxlingo/server/index.ts:~120` — Timing-safe comparison for API key  
> The `safeEqual` function uses `crypto.timingSafeEqual`, but only after converting to buffers. If the lengths differ, it returns false early, which is correct. However, ensure that both values are always strings and not undefined/null to avoid runtime errors.  
> **Suggestion:** Add explicit type checks or defaults to empty string.

---

**[Security] severity: medium**  
`voxlingo/server/index.ts:~130` — No authentication for health check endpoint  
> The `/health` endpoint is intentionally unauthenticated for load balancers, but this should be documented and reviewed to ensure no sensitive information is ever exposed here.  
> **Action:** Add a comment clarifying that only status is returned and no secrets are ever exposed.

---

**[Performance] severity: low**  
`voxlingo/hooks/useExplore.ts:~40` — AsyncStorage read is not debounced  
> Multiple rapid changes to destination/category could trigger overlapping AsyncStorage reads and fetches.  
> **Suggestion:** Consider debouncing or canceling previous requests if this becomes a performance issue in practice.

---

**[Error Handling] severity: medium**  
`voxlingo/hooks/useExplore.ts:~70` — Swallowed errors in AsyncStorage  
> The catch block for AsyncStorage read/write errors is empty, which could hide storage issues.  
> **Suggestion:** At least log these errors for debugging, even if they're non-critical.

---

**[Error Handling] severity: medium**  
`voxlingo/services/explore.ts:~13` — Error message may be unhelpful  
> If the server returns a non-JSON error (e.g., 500 with plain text), `await response.json()` will throw, masking the real error.  
> **Suggestion:** Add a fallback to handle non-JSON error responses.

---

**[Design] severity: low**  
`voxlingo/app/(tabs)/explore.tsx:~42` — `handleTranslate` always routes to `/(tabs)/`  
> The "Translate" button on a place card always routes to the root tabs, not specifically to the Travel tab or with context.  
> **Suggestion:** If possible, pass context or route directly to the Travel tab with the destination language pre-selected, as described in the spec.

---

**[Design] severity: low**  
`voxlingo/hooks/useExplore.ts:~18` — `category` is typed as `string | null`  
> For better type safety, consider using `ExploreCategoryId | null` instead of plain `string | null` throughout the hook and related components.

---

**[Bugs & Correctness] severity: low**  
`voxlingo/server/index.ts:~1100` — Unhandled promise rejections and uncaught exceptions  
> The process-level handlers log and exit, but in some environments (e.g., serverless), this may not be ideal.  
> **Suggestion:** Consider making this behavior configurable or document the rationale.

---

**[Bugs & Correctness] severity: low**  
`voxlingo/server/index.ts:~900` — No pagination on `/explore` endpoint  
> The backend returns all places (up to 20) and expects the frontend to paginate. This is fine for small lists, but if Gemini returns more, this could be a scalability issue.  
> **Suggestion:** If the number of places grows, consider adding server-side pagination.

---

**Summary:**  
The implementation is solid, with strong attention to caching, error handling, and security. The most significant issues are around API key enforcement, error handling for fetch failures, and minor type safety improvements. No major logic or architectural flaws were found.

**Verdict:** REQUEST_CHANGES  
**Risk level:** MEDIUM — Most issues are not likely to cause outages, but API key enforcement and error handling should be tightened before production.