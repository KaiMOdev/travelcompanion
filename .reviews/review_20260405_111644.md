# Code Review — last commit

**Model**: gpt-5.4  
**Date**: 2026-04-05 11:16

---

**[BUGS & CORRECTNESS] severity: medium**  
`voxlingo/app/(tabs)/camera.tsx:197` — `flash` prop was removed and replaced with `enableTorch`, which changes behavior from capture-time flash to continuous torch
> This is not a like-for-like replacement unless the intent is specifically to keep the light on while previewing. On many camera APIs, `flash` affects photo capture behavior, while `enableTorch` turns on the device torch continuously. That can change UX, battery usage, thermal behavior, and photo results.
> 
> Verify the `CameraView` API for the library/version in use. If the goal is still camera flash during capture, keep using the supported flash mode prop or pass the flash mode to the capture call instead. If the goal is torch, consider renaming the state from `flash` to `torchEnabled` to avoid misleading logic and future bugs.

**[DESIGN] severity: low**  
`voxlingo/app/(tabs)/camera.tsx:197` — State naming no longer matches behavior
> `flash` now controls `enableTorch`, which makes the code misleading for future maintainers. This increases the chance of incorrect assumptions elsewhere in the screen, especially around capture logic.
> 
> Suggested change:
> ```tsx
> const [torchEnabled, setTorchEnabled] = useState(false);
> ...
> <CameraView
>   ref={cameraRef}
>   style={styles.camera}
>   facing="back"
>   enableTorch={torchEnabled}
>   onCameraReady={() => setCameraReady(true)}
> />
> ```

**Summary**: Small diff, but it appears to change camera-light behavior rather than just updating an API prop. If intentional, the implementation should be clarified in naming; if not, this may introduce a functional regression.

**Verdict**: REQUEST_CHANGES  
**Risk level**: MEDIUM