# Code Review — commits HEAD~5..HEAD

**Model**: gpt-5.4  
**Date**: 2026-04-05 14:53

---

**[BUGS & CORRECTNESS] severity: high**  
`voxlingo/app/(tabs)/index.tsx:50` — destination change now overwrites a user-selected target language
> The previous behavior respected `targetLangManuallySet`; this effect now always resets `targetLang` to the destination language and then sets `targetLangManuallySet(false)`. That means if a user manually chose a different target language, any later destination reload/change will silently override it.
> 
> Restore the guard so destination changes only auto-apply when the user has not manually overridden the target language.
> 
> ```tsx
> useEffect(() => {
>   const langCode = getLanguageCode();
>   if (langCode && !targetLangManuallySet) {
>     setTargetLang(langCode);
>   }
>   clearTranslations();
> }, [destination, targetLangManuallySet]);
> ```

**[BUGS & CORRECTNESS] severity: medium**  
`voxlingo/app/(tabs)/index.tsx:79` — conversation recording state can become stale after press-out
> `conversationLangsRef.current` is never cleared in `handleMePressOut` / `handleThemPressOut`. The UI uses this ref to decide which button shows `⏹️`, so after recording stops, stale state may remain associated with the last side pressed until the next interaction. It also makes future logic more error-prone if `isRecording` briefly lags state updates.
> 
> Clear the ref after stopping.
> 
> ```tsx
> const handleMePressOut = () => {
>   if (conversationLangsRef.current) {
>     stopRecord(conversationLangsRef.current.source, conversationLangsRef.current.target);
>     conversationLangsRef.current = null;
>   }
> };
> ```

**[ERROR HANDLING] severity: medium**  
`voxlingo/hooks/useDestination.ts:39` — AsyncStorage write errors from hotel address updates are unhandled
> `setHotelAddress` awaits `AsyncStorage.setItem`, but if persistence fails, the promise rejects and can surface as an unhandled error from a text input change path. This is especially risky because it runs on every keystroke.
> 
> Catch storage failures and decide whether to surface a non-blocking error or silently keep the in-memory value.
> 
> ```ts
> const setHotelAddress = useCallback(async (address: string) => {
>   setHotelAddressState(address);
>   try {
>     await AsyncStorage.setItem(HOTEL_STORAGE_KEY, address);
>   } catch {
>     // optionally setError('Failed to save hotel address');
>   }
> }, []);
> ```

**[PERFORMANCE] severity: medium**  
`voxlingo/app/(tabs)/index.tsx:188` and `voxlingo/hooks/useDestination.ts:39` — persisting hotel address on every keystroke will cause excessive storage writes
> `TextInput.onChangeText` calls `setHotelAddress`, which writes to AsyncStorage for every character typed. That adds unnecessary I/O on the hot path, can cause input jank on lower-end devices, and increases wear on persistent storage.
> 
> Keep local state in the input and persist on blur, submit, or with a debounce.
> 
> ```tsx
> const [draftHotelAddress, setDraftHotelAddress] = useState(hotelAddress);
> 
> <TextInput
>   value={draftHotelAddress}
>   onChangeText={setDraftHotelAddress}
>   onBlur={() => setHotelAddress(draftHotelAddress)}
> />
> ```
> 
> Or debounce the storage write inside the hook.

**[DESIGN] severity: low**  
`voxlingo/app/(tabs)/index.tsx:50` — clearing translations on initial saved-destination load may unexpectedly wipe session history
> `loadSaved()` runs on mount, which triggers the `[destination]` effect and now always calls `clearTranslations()`. If translations are intended to persist during the current app session while saved destination is restored, this mount-time side effect will erase them even though the user did not actively change destination.
> 
> If the intent is to clear only on user-initiated destination/language changes, gate the effect with a “hasMounted” ref or move clearing into the explicit setters/handlers.

**Summary**: The diff adds useful UX improvements, especially conversation mode and no-speech handling, but it also introduces a regression in target-language selection and a couple of state/persistence issues. The storage path for hotel address should also be made safer and less chatty.

**Verdict**: REQUEST_CHANGES  
**Risk level**: MEDIUM