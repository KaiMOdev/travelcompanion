# Code Review — commits HEAD~2..HEAD

**Model**: gpt-5.4  
**Date**: 2026-04-05 11:17

---

**[BUGS & CORRECTNESS] severity: medium**  
`voxlingo/app/(tabs)/camera.tsx:200` — `flash` behavior changed from capture flash to continuous torch without validating platform/API support  
> The previous prop (`flash={flash ? 'on' : 'off'}`) controls photo flash behavior, while `enableTorch={flash}` turns on the device torch continuously during preview. That is a behavioral change, not just an API rename, and it can affect UX, battery usage, and image capture behavior. If the intent was to preserve still-photo flash, this likely changes functionality incorrectly.  
> Verify the `CameraView` API for your Expo SDK version and use the prop that matches the intended behavior. If torch is desired, consider renaming the state from `flash` to `torch` to avoid misleading logic.

```tsx
<CameraView
  ref={cameraRef}
  style={styles.camera}
  facing="back"
  flash={flash ? 'on' : 'off'} // if supported in your SDK and intended for capture flash
  onCameraReady={() => setCameraReady(true)}
/>
```

Or, if torch is the real intent:

```tsx
const [torchEnabled, setTorchEnabled] = useState(false);

<CameraView
  ref={cameraRef}
  style={styles.camera}
  facing="back"
  enableTorch={torchEnabled}
  onCameraReady={() => setCameraReady(true)}
/>
```

**[BUGS & CORRECTNESS] severity: low**  
`voxlingo/app/(tabs)/camera.tsx:109` — `mediaTypes` API change may be version-sensitive and can break on newer Expo ImagePicker versions  
> This change fixes compatibility for SDKs expecting `ImagePicker.MediaTypeOptions.Images`, but Expo has changed this API across versions. If the project is on a newer SDK where `MediaTypeOptions` is deprecated or replaced, this can introduce runtime/type issues.  
> Confirm the installed `expo-image-picker` version and align with its documented enum/value. If you're upgrading for compatibility, make sure the rest of the file and package versions are updated consistently.

**Summary**: Small diff, but the camera prop change appears to alter behavior rather than simply fixing an API mismatch. The ImagePicker change is probably fine if it matches the installed Expo version, but it should be verified against package/API compatibility.

**Verdict**: REQUEST_CHANGES  
**Risk level**: MEDIUM