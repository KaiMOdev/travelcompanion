# Code Review — commits HEAD~6..HEAD

**Model**: gpt-5.4  
**Date**: 2026-04-05 13:35

---

**[BUGS & CORRECTNESS] severity: high**  
`voxlingo/app/(tabs)/camera.tsx:126` — non-null assertion on `asset.base64` can crash or send invalid data
> `expo-image-picker` does not guarantee `asset.base64` unless the picker request explicitly asks for it. Using `asset.base64!` will either pass `undefined` into `translateImageSmart` or throw later in a hard-to-debug way.
> Ensure `base64: true` is set in the picker options and guard before calling the service.
> ```ts
> const result = await ImagePicker.launchImageLibraryAsync({
>   mediaTypes: ['images'],
>   quality: 0.8,
>   base64: true,
> });
> 
> const asset = result.assets?.[0];
> if (!asset?.base64) {
>   setError('Could not read image data');
>   return;
> }
> const visionResult = await translateImageSmart(asset.base64, targetLang);
> ```

**[BUGS & CORRECTNESS] severity: high**  
`voxlingo/server/index.ts:282-294` — `/vision` no longer validates `detectedLanguage` for menu/sign responses
> The old endpoint required `detectedLanguage`; the new branch-specific validation only checks `items` for menus and `originalText`/`translatedText` for signs. The UI unconditionally renders `result.detectedLanguage` in `MenuResult` and `SignResult`, so malformed model output will surface as `undefined.toUpperCase()` or broken UI.
> Keep `detectedLanguage` required for all content types before returning.
> ```ts
> if (!parsed.detectedLanguage) {
>   res.status(500).json({ error: 'Missing detectedLanguage in vision response' });
>   return;
> }
> 
> if (parsed.contentType === 'menu') {
>   if (!Array.isArray(parsed.items)) {
>     res.status(500).json({ error: 'Invalid menu response from Gemini' });
>     return;
>   }
> }
> ```

**[BUGS & CORRECTNESS] severity: medium**  
`voxlingo/components/TaxiCard.tsx:18-31` — stale location/error state persists across modal reopen and failed retries
> `loadLocation()` sets `error` but does not clear a previous `location`, and successful loads do not clear stale data before the request starts. This can show an old address while a new request is in flight or after a failure.
> Reset transient state at the start of each load.
> ```ts
> const loadLocation = async () => {
>   setIsLoading(true);
>   setError(null);
>   setLocation(null);
>   try {
>     const loc = await getCurrentLocation();
>     if (!loc) {
>       setError('Could not get your location. Please enable location services.');
>       return;
>     }
>     setLocation(loc);
>   } finally {
>     setIsLoading(false);
>   }
> };
> ```

**[ERROR HANDLING] severity: medium**  
`voxlingo/components/EmergencyCard.tsx:12-14` — phone/advisory links are opened without failure handling
> `Linking.openURL` can reject if the device cannot handle the URL or permissions/settings block the action. In an emergency flow, silent failure is a bad UX.
> Check support with `canOpenURL` and surface an error or fallback.
> ```ts
> const callNumber = async (number: string) => {
>   const url = `tel:${number}`;
>   if (await Linking.canOpenURL(url)) {
>     await Linking.openURL(url);
>   } else {
>     // show alert/banner
>   }
> };
> ```

**[ERROR HANDLING] severity: medium**  
`voxlingo/hooks/useDestination.ts:34-42` and `voxlingo/app/(tabs)/index.tsx` — destination loading errors are captured but never shown
> `useDestination` exposes `error`, but `TravelScreen` never renders it. If phrase/tip generation fails, the user just sees empty content with no explanation.
> Either display the hook error via `ErrorBanner` or merge it into the existing screen-level error handling.
> ```tsx
> const { error: destinationError, ... } = useDestination();
> 
> {(error || destinationError) && (
>   <ErrorBanner
>     message={error || destinationError!}
>     onDismiss={() => {
>       clearError();
>       // add clearDestinationError()
>     }}
>   />
> )}
> ```

**[DESIGN] severity: medium**  
`voxlingo/app/(tabs)/index.tsx:49-56` — auto-overwriting `targetLang` on destination change can clobber explicit user choice
> The effect always forces `targetLang` to the destination’s primary language. If a user manually selects a different target language after setting a destination, any later destination reload/change will silently override that preference.
> Track whether the language was auto-set vs manually chosen, or only set the target language when destination is first selected and the user hasn’t changed it.
> ```ts
> const [targetLangManuallyChanged, setTargetLangManuallyChanged] = useState(false);
> 
> useEffect(() => {
>   const langCode = getLanguageCode();
>   if (langCode && !targetLangManuallyChanged) {
>     setTargetLang(langCode);
>   }
> }, [destination, targetLangManuallyChanged, getLanguageCode]);
> ```

**[PERFORMANCE] severity: low**  
`voxlingo/components/DestinationPicker.tsx:17-28` — filtering and sorting run on every render/keystroke without memoization
> The list is small today, so this is not urgent, but the component does a full filter + copy + sort every render. Since this modal is search-driven, memoizing the derived list is straightforward and avoids unnecessary work as the destination list grows.
> ```ts
> const sorted = useMemo(() => {
>   const filtered = search
>     ? DESTINATIONS.filter((d) =>
>         d.countryName.toLowerCase().includes(search.toLowerCase()))
>     : DESTINATIONS;
> 
>   return [...filtered].sort((a, b) => {
>     if (a.isHero && !b.isHero) return -1;
>     if (!a.isHero && b.isHero) return 1;
>     return a.countryName.localeCompare(b.countryName);
>   });
> }, [search]);
> ```

**Summary**: The feature set is substantial and the UI additions are coherent, but there are a couple of correctness gaps around image base64 handling and `/vision` response validation that can break core flows. Error handling is also uneven in a few user-critical paths.

**Verdict**: REQUEST_CHANGES  
**Risk level**: MEDIUM — the diff is promising, but the camera import path and smart vision response assumptions are likely to cause production issues.