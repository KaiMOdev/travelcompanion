# Code Review — staged changes

**Model**: gpt-5.4  
**Date**: 2026-04-05 11:27

---

**[BUGS & CORRECTNESS] severity: medium**  
`voxlingo/components/RecordButton.tsx:74` — Button remains pressable while translating, despite UI indicating a busy state
> The component shows `⏳` and “Translating...”, but `onPress` is still wired and there’s no disabled guard. That can trigger duplicate actions or state transitions while a translation request is already in flight.
> 
> Disable interaction during translation (and optionally while recording, depending on intended UX).
> ```tsx
> <AnimatedPressable
>   disabled={isTranslating}
>   onPress={isTranslating ? undefined : onPress}
>   style={[
>     styles.button,
>     isRecording && styles.recording,
>     isTranslating && styles.translating,
>     { transform: [{ scale: pulseAnim }] },
>   ]}
> >
> ```
> If you want visual feedback, also add a disabled style.

**[BUGS & CORRECTNESS] severity: medium**  
`voxlingo/app/(tabs)/camera.tsx:200` — Removing `flash` prop may change capture behavior from photo flash to continuous torch
> This change is not just a rename: the previous code set both `flash={flash ? 'on' : 'off'}` and `enableTorch={flash}`; now only `enableTorch` remains. On camera APIs, flash-for-capture and torch-preview are often distinct behaviors. If users expect brighter still photos rather than a continuously lit preview, this will regress functionality.
> 
> Confirm the intended behavior and keep the capture flash prop if still needed:
> ```tsx
> <CameraView
>   ref={cameraRef}
>   style={styles.camera}
>   facing="back"
>   flash={torchEnabled ? 'on' : 'off'}
>   enableTorch={torchEnabled}
>   onCameraReady={() => setCameraReady(true)}
> />
> ```
> If the goal is truly torch-only, rename the UI affordance/text accordingly so the behavior is explicit.

**Summary**: The diff is small and mostly reasonable, but it introduces a likely UX/state bug in the record button and may unintentionally change camera lighting behavior from flash to torch-only. Both should be clarified before merging.

**Verdict**: REQUEST_CHANGES  
**Risk level**: MEDIUM